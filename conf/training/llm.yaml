optimizer: adamw
lr: 1e-4
weight_decay: 0.01
warmup_steps: 500
max_grad_norm: 1.0
batch_size: 2
grad_accum_steps: 8
epochs: 30
early_stopping_patience: 3
early_stopping_metric: val_loss
wer_eval_every_n_epochs: 5
log_every_n_steps: 10
val_every_n_epochs: 1
